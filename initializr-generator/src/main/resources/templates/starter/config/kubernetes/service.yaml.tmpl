apiVersion: v1
kind: Service
metadata:
  name: {{artifactId}}
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    run: {{artifactId}}
    version: $version
  type: ClusterIP
